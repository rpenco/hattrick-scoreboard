<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/RelativeLayout1"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="5dp">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Défense latérale droite" />

    <TextView
        android:id="@+id/tvAwayLevel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/tvHomeLevel"
        android:layout_alignBottom="@+id/tvHomeLevel"
        android:layout_alignParentRight="true"
        android:text="Faible"
        android:textColor="@color/text_gray" />

    <TextView
        android:id="@+id/tvHomeLevel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/tvTitle"
        android:layout_below="@+id/tvTitle"
        android:text="Passable"
        android:textColor="@color/text_gray" />

    <ImageView
        android:id="@+id/ivProgress"
        android:layout_width="wrap_content"
        android:layout_height="10dp"
        android:layout_alignParentLeft="true"
        android:layout_alignRight="@+id/tvAwayLevel"
        android:layout_below="@+id/tvHomeLevel"
        android:scaleType="fitXY"
        android:src="@drawable/ic_injury" />

</RelativeLayout>